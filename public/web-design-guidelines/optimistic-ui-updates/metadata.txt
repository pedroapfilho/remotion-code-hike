Optimistic UI Updates

Update the UI immediately before the server responds, then reconcile or rollback on error. This makes interactions feel instant. Show a subtle error message and undo the change if the request fails.
