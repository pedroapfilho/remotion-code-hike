ARIA Labels for Icons

Icon-only buttons must have aria-label describing the action. Mark decorative icons as aria-hidden="true" so screen readers skip them. The label should describe what happens when clicked, not what the icon looks like.
