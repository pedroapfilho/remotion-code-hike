SWR Request Deduplication

Use SWR or React Query to deduplicate identical requests. Multiple components requesting the same data should share a single network request instead of making redundant calls.