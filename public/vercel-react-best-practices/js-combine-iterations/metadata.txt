Combine Iterations

Combine multiple array iterations into one pass when possible. Chaining map().filter().reduce() creates intermediate arrays - combine operations to iterate once and reduce memory allocation.