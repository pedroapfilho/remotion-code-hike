Parallel Data Fetching

Use component composition to enable parallel fetching. When you await in a parent before rendering children, fetches run sequentially. Move fetches into sibling components so React can execute them simultaneously.
