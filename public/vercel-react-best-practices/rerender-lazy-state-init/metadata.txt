Lazy State Initialization

Pass a function to useState for expensive initial values. useState(() => expensiveComputation()) only runs once, while useState(expensiveComputation()) runs on every render.